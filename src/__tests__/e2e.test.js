const Injector = require('../Injector');

describe('e2e', () => {
  it('should return a proxied instance', () => {});
  it('should return a insertion query on the proxied instance', () => {});
  it('should return a updation query on the proxied instance', () => {});
  it('should return a deletion query on the proxied instance', () => {});
  it('should return a selection query on the proxied instance', () => {});
});
